export function questStr() {
    const strResult = player1.attributes[0] + rollDice20();
    if (strResult >= 12) {
        // success in resultSpan
    } else {
        // failure in resultSpan
    }
}

export function questInt() {
    const intResult = player1.attributes[1] + rollDice20();
    classStats
}

export function questDex() {
    const classStats = player1.attributes[2] + rollDice20();
    classStats
}